import {
  ChainId as LifiChainId,
  ChainKey as LifiChainKey,
  CoinKey as LifiCoinKey,
} from '@lifi/types'

export type ChainId = LifiChainId

export type ChainKey = LifiChainKey

export type CoinKey = LifiCoinKey
